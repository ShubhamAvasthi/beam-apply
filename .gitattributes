# Normalize all text files to LF in the repo and the working tree.
# This pins line endings independent of each machine's core.autocrlf,
# so Prettier's `endOfLine: "lf"` check survives fresh checkouts.
* text=auto eol=lf
